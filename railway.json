{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn plagenor.wsgi --bind 0.0.0.0:$PORT --workers 3",
    "preDeployCommand": "python manage.py migrate --no-input && python manage.py seed_services && python manage.py seed_content && python manage.py ensure_superuser",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
